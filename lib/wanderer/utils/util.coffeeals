{random,sqrt,min,max} = Math

extend = (obj, mixin) ->
  obj[name] = method for name, method of mixin
  obj

global.include = (klass, mixin) ->
  extend klass.prototype, mixin

global.randint = (from,to)->
  unless to?
    to = from
    from = 0
  ~~(random()*(to-from+1))+from
